<h1>Select a Clinic</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clinics.each do |clinic| %>
      <tr>
        <td><%= clinic.name %></td>
        <td><%= link_to 'Show Patients', clinic %></td>
        <td><%= link_to 'Edit Clinic', edit_clinic_path(clinic) %></td>
        <td><%= link_to 'Delete Clinic', clinic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Clinic', new_clinic_path %>
