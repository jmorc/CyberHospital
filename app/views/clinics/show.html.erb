<p id="notice"><%= notice %></p>

<h1>Welcome to the <%= @clinic.name %> clinic</h1>
<h2>Patients:</h2>
<% @clinic.patients.each do |patient| %>
  <li><%= patient.name %> <%= link_to 'View Chart', patient %> | 
  	<%= link_to "Edit", edit_clinic_patient_url(@clinic, patient) %> | 
    <%= link_to "Delete", patient_url(patient), method: :delete %>
  </li>
<% end %>

<%= link_to 'Add Patients to this Clinic', new_clinic_patient_url(@clinic) %> |
<%= link_to 'Edit', edit_clinic_url(@clinic) %> |
<%= link_to 'Back', clinics_url %>
